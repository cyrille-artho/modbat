language: scala
before_install: sudo apt-get install scala
scala:
   - 2.11.11
script: find / -name scala
#./gradlew buildDependents && ls -lR && (bash bin/test.sh; find . -name '*.log' -o -name '*.err' | sed -e 's/\(.*\)/echo \1; cat \1/' | sh)
